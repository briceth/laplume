<div class="message-box">
 <div class="container">
    <div class="row">
      <div class="col-xs-4 col-xs-offset-4">
        <%= simple_form_for [@user, @offer] do |f| %>
          <%= f.input :mission, collection: @missions %>
          <%= f.button :submit %>
        <% end %>
      </div>
    </div>
  </div>
</div>
